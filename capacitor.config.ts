import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hamadh.wififriendFinder',
  appName: 'WiFi Friend Finder',
  webDir: 'dist'
};

export default config;
